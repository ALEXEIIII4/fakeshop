logging:
  level:
    org.springframework.security: trace

spring:
  liquibase:
    enabled: true
    change-log: db.changelog/changelog-master.yaml


  datasource:
    url: jdbc:postgresql://localhost:5434/demo_shop
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver